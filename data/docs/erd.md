# Entity-Relationship Diagram

_Auto-generated by `python -m scripts.dump_erd`. Do not edit by hand._

```mermaid
erDiagram
    graduation_rule ||--o{ graduation_rule : "parent_rule_id"
    student ||--o{ enrollment : "student_id"
    course ||--o{ enrollment : "course_id"

    course {
        varchar(20) course_id PK
        varchar(200) name
        decimal(3_1) credits
        enum category
        varchar(50) sub_category
    }

    graduation_rule {
        int rule_id PK
        varchar(50) category
        varchar(50) sub_category
        varchar(20) rule_type
        int min_credits
        int max_credits
        int parent_rule_id FK
    }

    student {
        int student_id PK
        varchar(100) name
        varchar(255) hashed_password
    }

    enrollment {
        int enrollment_id PK
        int student_id FK
        varchar(20) course_id FK
        varchar(10) semester
    }
```

## Unique constraints

- `enrollment` unique `uq_student_course_semester`: (student_id, course_id, semester)
